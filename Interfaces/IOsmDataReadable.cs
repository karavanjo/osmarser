using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Osm
{
    /// <summary>
    /// Defines the signature of a class that reads the data files OSM (.osm, .pdb etc.) and transforms them into usable form for the class, importing data into DB
    /// </summary>
    public interface IOsmDataReadable
    {

    }
}
